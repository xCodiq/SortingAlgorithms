# before_init = int(time.time() * 1000)
# init()
# print(f" -> Successfully initialized in {int(time.time() * 1000) - before_init}ms")

# Creation of the files, only if they don't exist yet.
# before_creation = int(time.time() * 1000)
# create_file("random_list", rand_list)
# create_file("rev_list", rev_list)
# create_file("near_sort_list", near_sort_list)
# print(f" -> Successfully created all files in {int(time.time() * 1000) - before_creation}ms")
#
# print("Done!")
